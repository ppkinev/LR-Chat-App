export const initialState = { isFetching: false }
export const isAuthorized = (state = initialState) => state.isAuthorized
export const isLoginFetching = (state = initialState) => state.isLoginFetching
export const isGetMeFetching = (state = initialState) => state.isGetMeFetching
export const getPoints = (state = initialState) => state.points
export const getName = (state = initialState) => state.name
export const getLevel = (state = initialState) => state.level
export const getImage = (state = initialState) => state.image
export const getError = (state = initialState) => state.error
